mutation ForgetPassword($email: String!, $newPassword: String!) {
    forgetPassword(
        email: $email
        newPassword: $newPassword
    ) {
        status
        message
        user {
            id
            name
            email
            childname
            childage
            auth
        }
    }
}
