query GetUserByToken($auth: String!) {
    getUserByToken(auth: $auth) {
        status
        message
        user {
            id
            name
            image
            email
            childname
            childage
            auth
        }
    }
}
