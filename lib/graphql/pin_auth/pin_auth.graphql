mutation SetPin($auth: String!, $pin: String!) {
    setPin(auth: $auth, pin: $pin) {
        status
        message
        user {
            id
            name
            auth
            setUserPin
        }
    }
}

mutation LoginWithPin($auth: String!, $pin: String!) {
    loginWithPin(auth: $auth, pin: $pin) {
        status
        message
        user {
            id
            name
            auth
        }
    }
}

mutation ResetPin($auth: String!, $newPin: String!) {
    resetPin(auth: $auth, newPin: $newPin) {
        status
        message
        user {
            id
            name
            auth
        }
    }
}

mutation DisablePin($auth: String!) {
    disablePin(auth: $auth) {
        status
        message
        user {
            id
            name
            auth
            setUserPin
        }
    }
}

mutation ActivatePin($auth: String!) {
    activatePin(auth: $auth) {
        status
        message
        user {
            id
            name
            auth
            setUserPin
        }
    }
}

query VerifyPinStatus($auth: String!) {
    verifyPinStatus(auth: $auth) {
        status
        message
        pinStatus
        setUserPin
    }
}
