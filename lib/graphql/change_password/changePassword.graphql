mutation ChangePassword($auth: String!, $oldPassword: String!, $newPassword: String!) {
    changePassword(
        auth: $auth
        oldPassword: $oldPassword
        newPassword: $newPassword
    ) {
        status
        message
        user {
            id
            name
            email
            childname
            childage
            auth
        }
    }
}
